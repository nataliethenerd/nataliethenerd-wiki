git checkout main
git pull origin main
git add .
git commit -m "Updated"
git push origin main
set GIT_USER=nataliethenerd / $env:GIT_USER = "your-github-username"
npm run deploy 

add cname to gh-pages


npm run start